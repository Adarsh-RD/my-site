# Get a free API key at https://resend.com/api-keys
# Add the same variable in Vercel → Project → Settings → Environment Variables, then redeploy.
RESEND_API_KEY=re_xxxxxxxx

# Optional: use your verified domain sender (after verifying in Resend)
# RESEND_FROM=Adarsh Portfolio <contact@adarsh.lol>
